# 実行環境の設定 (development/test/production)
NODE_ENV=development
PORT=5000

# 共通データベース設定
DB_HOST=localhost
DB_PORT=3306
DB_USER=server_inspector
DB_PASSWORD=your_password

# 開発環境用データベース (development)
DEV_DB_NAME=server_inspection_dev_db

# テスト環境用データベース (test)
TEST_DB_NAME=server_inspection_test_db

# 本番環境用データベース (production)
PROD_DB_NAME=server_inspection_db

# 環境切り替えに関わらず特定のデータベースを使用する場合はこちらを設定
# DB_NAME=server_inspection_custom_db

# Migration & Sync Options
AUTO_MIGRATE=false  # true: 起動時に自動的にマイグレーションを実行
DB_SYNC=false       # 開発時のみ: モデル定義からDBを自動同期（注意: 本番では使用しないでください）

# -----------------------------------------------------
# データベース環境設定ガイド
# -----------------------------------------------------
# 1. 開発環境でテスト用DBを使用する場合:
#    NODE_ENV=development
#    DEV_DB_NAME=server_inspection_dev_db
#
# 2. 製品環境で本番用DBを使用する場合:
#    NODE_ENV=production
#    PROD_DB_NAME=server_inspection_db
#
# 3. 環境に関わらず特定のDBを使用する場合は以下を設定:
#    DB_NAME=任意のデータベース名
#    (この設定は環境別DB設定より優先されます)
#
# 4. コマンドラインから環境を変更して実行する例:
#    $ cross-env NODE_ENV=production npm run dev
#    $ npm run start:dev  # 開発環境スタート
#    $ npm run start:prod # 本番環境スタート
# -----------------------------------------------------