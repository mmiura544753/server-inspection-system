// server/routes/inspectionRoutesSQL.js
// 注意: このファイルは現在使用されていません。代わりにinspectionRoutes.jsが使用されています。
// このファイルは過去のSQL直接実行バージョンのルートで、保守のためだけに残されています。
// 実際にAPIに利用されるのはORMバージョンのinspectionRoutes.jsです。

const express = require("express");
const router = express.Router();

// 未使用のため空のルーターを返す
module.exports = router;
